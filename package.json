{
  "scripts": {
    "dev": "bun run --hot src/index.tsx",
    "start": "bun install && bun build src/index.tsx --target=bun --outfile=dist/server.js && bun run dist/server.js"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.583.0",
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}